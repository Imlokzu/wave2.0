{
  "name": "wavechat",
  "version": "1.0.0",
  "description": "WaveChat - Modern real-time chat application",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "install:backend": "cd backend && npm install"
  },
  "keywords": ["chat", "realtime", "socket.io", "ai"],
  "license": "MIT"
}
